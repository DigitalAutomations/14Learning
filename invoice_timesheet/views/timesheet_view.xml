<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="edit_project_inh" model="ir.ui.view">
        <field name="name">project.project.form</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="invoiceable"/>
                <field name="product_id" attrs="{'invisible': [('invoiceable','=',False)]}"/>
            </field>
        </field>
    </record>

    <record id="hr_timesheet_line_inv_inh" model="ir.ui.view">
        <field name="name">account.analytic.line</field>
        <field name="model">account.analytic.line</field>
        <field name="inherit_id" ref="hr_timesheet.hr_timesheet_line_tree"/>
        <field name="arch" type="xml">
            <field name="task_id" position="after">
                <field name="invoiceable"/>
            </field>
        </field>
    </record>

    <record id="hr_timesheet_line_tree" model="ir.ui.view">
        <field name="name">account.analytic.line.tree.hr_timesheet</field>
        <field name="model">account.analytic.line</field>
        <field name="arch" type="xml">
            <tree string="Timesheet Activities" create="false">
                <field name="date"/>
                <field name="user_id" required="1" options='{"no_open": True}'/>
                <field name="name"/>
                <field name="project_id" required="1"/>
                <field name="task_id" context="{'default_project_id': project_id}" domain="[('project_id', '=', project_id)]"/>
                <field name="invoiceable"/>
                <field name="unit_amount" string="Duration" sum="Total time" widget="float_time"/>
            </tree>
        </field>
    </record>

    <record id="hr_timesheet_line_form" model="ir.ui.view">
        <field name="name">account.analytic.line.form.inherit.hr_timesheet</field>
        <field name="model">account.analytic.line</field>
        <field name="inherit_id" ref="hr_timesheet.hr_timesheet_line_form"/>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <field name="date" position="after">
                <field name="invoiceable"/>
                <field name="acs_invoice_id"/>
            </field>
        </field>
    </record>

    <record id="hr_timesheet_line_search" model="ir.ui.view">
        <field name="name">account.analytic.line.search</field>
        <field name="model">account.analytic.line</field>
        <field name="arch" type="xml">
            <search string="Timesheet">
                <field name="date"/>
                <field name="user_id"/>
                <field name="project_id"/>
                <field name="task_id"/>
                <field name="name"/>
                <field name="department_id"/>
                <field name="acs_invoice_id"/>
                <separator/>
                <filter string="Invoiceable" name="invoiceable" domain="[('invoiceable','=',True)]"/>
                <filter string="Not Invoiced" name="not_invoiced" domain="[('acs_invoice_id','=',False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Users" name="groupby_user" domain="[]" context="{'group_by':'user_id'}"/>
                    <filter string="Project" name="groupby_project" domain="[]" context="{'group_by':'project_id'}"/>
                    <filter string="Task" name="groupby_task" domain="[]" context="{'group_by':'task_id'}"/>
                    <filter string="Product" name="groupby_product" domain="[]" context="{'group_by':'product_id'}"/>
                    <filter string="Timesheet Month" name="groupby_month" domain="[]" context="{'group_by':'date'}" help="Timesheet by Month"/>
                </group>
            </search>
        </field>
    </record>

    <record id="act_hr_timesheet_line" model="ir.actions.act_window">
        <field name="name">Activities</field>
        <field name="res_model">account.analytic.line</field>
        <field name="domain">[('invoiceable', '=', True)]</field>
        <field name="context">{"search_default_not_invoiced":1}</field>
        <field name="search_view_id" ref="hr_timesheet_line_search"/>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Click to record activities.
          </p><p>
            You can register and track your workings hours by project every
            day. Every time spent on a project will become a cost and can be re-invoiced to
            customers if required.
          </p>
        </field>
    </record>

    <record model="ir.actions.act_window.view" id="act_hr_timesheet_line_view_tree">
        <field name="view_mode">tree</field>
        <field name="sequence">1</field>
        <field name="view_id" ref="hr_timesheet_line_tree"/>
        <field name="act_window_id" ref="act_hr_timesheet_line"/>
    </record>

    <record model="ir.actions.act_window.view" id="act_hr_timesheet_line_view_form">
        <field name="view_mode">form</field>
        <field name="sequence">2</field>
        <field name="view_id" ref="hr_timesheet_line_form"/>
        <field name="act_window_id" ref="act_hr_timesheet_line"/>
    </record>

    <menuitem id="menu_hr_invoice_timesheet" sequence="60" parent="hr_timesheet.timesheet_menu_root" action="act_hr_timesheet_line" name="Invoice Timesheet"/>

    <menuitem id="menu_hr_invoice_activity" parent="menu_hr_invoice_timesheet" name="Invoice Activities"/>

</odoo>
